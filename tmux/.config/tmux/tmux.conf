# list of plugins
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'aserowy/tmux.nvim'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'

# Configure Catppuccin

# Configure Online
set -g @online_icon "ok"
set -g @offline_icon "nok"

# status left look and feel
set -g status-left-length 100
set -g status-left ""
set -ga status-left "#{?client_prefix,#{#[bg=red,fg=default,bold]  #S },#{#[bg=default,fg=green]  #S }}"
set -ga status-left "#[bg=default,fg=brightblack,none]│"
set -ga status-left "#[bg=default,fg=red]  #{pane_current_command} "
set -ga status-left "#[bg=default,fg=brightblack,none]│"
set -ga status-left "#[bg=default,fg=blue]  #{=/-32/...:#{s|$USER|~|:#{b:pane_current_path}}} "
set -ga status-left "#[bg=default,fg=brightblack,none]#{?window_zoomed_flag,│,}"
set -ga status-left "#[bg=default,fg=yellow]#{?window_zoomed_flag,  zoom ,}"

# status right look and feel
set -g status-right-length 100
set -g status-right ""
set -ga status-right "#{?#{e|>=:10,#{battery_percentage}},#{#[bg=red,fg=default]},#{#[bg=default,fg=magenta]}} #{battery_icon} #{battery_percentage} "
set -ga status-right "#[bg=default,fg=brightblack, none]│"
set -ga status-right "#[bg=default]#{?#{==:#{online_status},ok},#[fg=magenta] 󰖩 on ,#[fg=red,bold]#[reverse] 󰖪 off }"
set -ga status-right "#[bg=default,fg=brightblack, none]│"
set -ga status-right "#[bg=default,fg=blue] 󰭦 %m-%d-%Y 󰅐 %I:%M %p "

# Configure Tmux
set -g status-position top
set -g status-style "bg=default"
set -g status-justify "absolute-centre"

# pane border look and feel
setw -g pane-border-status top
setw -g pane-border-format ""
setw -g pane-active-border-style "bg=default,fg=brightblack"
setw -g pane-border-style "bg=default,fg=brightblack"
setw -g pane-border-lines single

# window look and feel
set -wg automatic-rename on
set -g automatic-rename-format "Window"

set -g window-status-format " #I#{?#{!=:#{window_name},Window},: #W,} "
set -g window-status-style "bg=default,fg=white"
set -g window-status-last-style "bg=default,fg=yellow"
set -g window-status-activity-style "bg=red,fg=default"
set -g window-status-bell-style "bg=red,fg=default,bold"
set -gF window-status-separator "#[bg=default,fg=brightblack]│"

set -g window-status-current-format " #I#{?#{!=:#{window_name},Window},: #W,} "
set -g window-status-current-style "bg=yellow,fg=default,bold"

set -g @sessionx-bind 's'

set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:RGB"

# Rename your terminals
set -g set-titles on

# move status bar to top
set-option -g status-position top

# set -g status-utf8 on
set -g status-interval 5
set -g status-left-length 90
set -g status-right-length 60
set-option -g default-shell /bin/bash

# Toggle tmux status bar
bind '\' set status
if-shell "[[ $(tmux lsw | wc -l) -le 1 ]]" 'set -g status'

set -g status on

# Enable mouse
set -g mouse on

# Increase history limit
set -g history-limit 30000

# Start windows and panes at 1 and not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Zero-out escape time delay for quicker response
set -s escape-time 0

# prefix
set -g prefix C-Space
unbind C-b
bind-key C-Space send-prefix

# vi copy mode
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection

# keybindings
bind-key r source-file ~/.config/tmux/tmux.conf \; display-message "~/.config/tmux/tmux.conf reloaded"

# resizing
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"

bind -n 'M-h' if-shell "$is_vim" 'send-keys M-h' 'resize-pane -L 1'
bind -n 'M-j' if-shell "$is_vim" 'send-keys M-j' 'resize-pane -D 1'
bind -n 'M-k' if-shell "$is_vim" 'send-keys M-k' 'resize-pane -U 1'
bind -n 'M-l' if-shell "$is_vim" 'send-keys M-l' 'resize-pane -R 1'

bind-key -T copy-mode-vi M-h resize-pane -L 1
bind-key -T copy-mode-vi M-j resize-pane -D 1
bind-key -T copy-mode-vi M-k resize-pane -U 1
bind-key -T copy-mode-vi M-l resize-pane -R 1

# Open the new panes in the current path
bind v split-window -v -c "#{pane_current_path}"
bind h split-window -h -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Auto-install TPM if not present
if "test ! -d ~/.config/tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm && ~/.config/tmux/plugins/tpm/bin/install_plugins'"

# Initialize TPM (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:Tc"
